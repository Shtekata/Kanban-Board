import { ShortFormatTextPipe } from './short-format-text.pipe';

describe('ShortFormatTextPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortFormatTextPipe();
    expect(pipe).toBeTruthy();
  });
});
