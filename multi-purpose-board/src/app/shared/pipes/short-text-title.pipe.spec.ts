import { ShortTextTitlePipe } from './short-text-title.pipe';

describe('ShortTextTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new ShortTextTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
