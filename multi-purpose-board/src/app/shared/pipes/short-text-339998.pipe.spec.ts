import { ShortText339998Pipe } from './short-text-339998.pipe';

describe('ShortText3399.98Pipe', () => {
  it('create an instance', () => {
    const pipe = new ShortText339998Pipe();
    expect(pipe).toBeTruthy();
  });
});
