import { ShortText3399982Pipe } from './short-text-339998-2.pipe';

describe('ShortText3399982Pipe', () => {
  it('create an instance', () => {
    const pipe = new ShortText3399982Pipe();
    expect(pipe).toBeTruthy();
  });
});
